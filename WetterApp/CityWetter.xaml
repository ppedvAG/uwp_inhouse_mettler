<UserControl
    x:Class="WetterApp.CityWetter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WetterApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock Text="{x:Bind Stadt,Mode=OneWay}"  />
        <StackPanel Grid.Column="1">
            <TextBlock><Run Text="{x:Bind Temperatur,Mode=OneWay}" />°C</TextBlock>
            <TextBlock Text="{x:Bind Beschreibung,Mode=OneWay}" />
        </StackPanel>
        <Image Source="{x:Bind IconUrl,Mode=OneWay}" Grid.Column="2" HorizontalAlignment="Stretch" 
               VerticalAlignment="Stretch" Stretch="UniformToFill" />

    </Grid>
</UserControl>
